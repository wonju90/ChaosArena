# ChaosArena 발표/시연 가이드 (사내 데모·스터디, 10~15분)

> 이 문서는 다른 4개 문서와 목적이 다르다 — **"실제로 발표 자리에서 몇 분에 뭘 보여주고
> 무슨 말을 할지"** 만 다룬다. 기술적 근거가 필요하면 각 절의 링크를 따라
> `PROJECT_LOG.md`(히스토리)/`CONCEPTS.md`(설계 이유)/`ARCHITECTURE.md`(시나리오 흐름)로
> 넘어가면 된다.

전제: 라이브 시연(실제 클러스터), 청중은 사내 동료(개발자), 발표 시간 10~15분.

---

## 1. 전체 타임라인 (13분 기준, 여유 2분은 Q&A/버퍼)

| 시간 | 구간 | 내용 |
|---|---|---|
| 0:00–1:00 | 후크 | 게임 콘솔에서 Chaos 버튼 → 파드 즉시 자동 복구 + 랭크 |
| 1:00–3:00 | 배경 | 왜 만들었는지, 무엇을 배우고 싶었는지 |
| 3:00–5:00 | 아키텍처 | 다이어그램 한 장으로 전체 그림 |
| 5:00–11:00 | 메인 라이브 데모 | 리전 강제 다운 → Self-Healing + GSLB 페일오버 |
| 11:00–13:00 | 트러블슈팅 하이라이트 | 스토리 1~2개 |
| 13:00–15:00 | 회고 + Q&A | 다음 계획, 질문 받기 |

**원칙: 데모가 주인공, 말은 데모를 설명하는 보조 수단.** 슬라이드로 아키텍처를 길게
설명하려 하지 말고, 되도록 빨리 화면(브라우저)으로 넘어갈 것 — 발표를 들어본 경험상
"설명 먼저, 데모 나중"보다 "데모 먼저, 설명은 기다리는 동안"이 훨씬 안 지루하다.

---

## 2. 구간별 대본 포인트

### 2.1 후크 (0:00–1:00) — `/` 게임 콘솔

- 바로 열어서: "이게 오늘 보여드릴 프로젝트 전체를 한 줄로 요약한 겁니다." 라고 말하며
  Chaos 버튼(이지 모드) 클릭.
- 파드가 죽고 몇 초 안에 새 파드가 뜨는 것 + 랭크(S/A/B/C)가 화면에 뜨는 것을 보여줌.
- 한 줄 정리: "쿠버네티스가 원래 하는 self-healing을, 죽이는 것부터 복구 시간 측정까지
  게임처럼 체험할 수 있게 만든 겁니다. 오늘은 이걸 리전 단위로 확장한 부분까지 보여드릴게요."

### 2.2 배경 (1:00–3:00)

핵심 메시지 하나만: **"이 프로젝트의 목적은 기능 구현이 아니라, 인프라를 직접 만들면서
실제로 부딪힌 문제를 어떻게 풀었는지 남기는 것"** — `PROJECT_LOG.md`에 트러블슈팅만 38개
절 있다는 걸 화면에 스크롤 한 번 보여주는 것만으로도 효과적.

말할 것:
- 관리형 K8s(NKS) 대신 자체 kubeadm 구축을 선택한 이유(더 배울 게 많아서) — [ARCHITECTURE.md](../ARCHITECTURE.md) 3.1절
- 단일 클러스터가 아니라 KR1/KR2 멀티 리전 + GSLB로 간 이유(Active 리전 자체가 통째로 죽는
  상황까지 다루고 싶어서)

### 2.3 아키텍처 한 장 (3:00–5:00)

- `docs/ARCHITECTURE.md` 0절의 mermaid 다이어그램을 화면에 띄우고 흐름만 손으로 짚기:
  사용자 → GSLB → KR1/KR2 각각 Ingress→Pod, 그 옆에 Jenkins→NCR→ArgoCD→Pod (CI/CD),
  Prometheus→Alertmanager→Slack.
- 강조 포인트 1개만: **"KR1과 KR2가 완전히 대칭 구조입니다. Jenkins/ArgoCD/모니터링이
  양쪽에 각각 다 있어요 — 안 그러면 KR2(원래 메인)가 죽었을 때 배포 자체를 못 하는
  단일장애점(SPOF)이 되니까요."** (컨트롤 플레인 이중화, 4.34절 — 실제로 이 문제를 겪고
  나서 고친 것이라고 언급하면 좋음)
- 여기서 절대 모든 컴포넌트를 하나하나 설명하려 하지 말 것 — 2분 예산 초과의 가장 흔한 원인.

### 2.4 메인 라이브 데모 (5:00–11:00) — `/infra` 탭

**목표: "리전 헬스체크(수 초)"와 "GSLB 실제 전환(TTL 때문에 30~80초)"이 서로 다른
타임스케일이라는 것 자체를 보여주는 것.** 이게 이 데모의 유일한 핵심 포인트고, 나머지는
곁다리다.

1. `/infra` 탭을 미리 열어두고 화면 공유 시작.
2. "지금 KR1이 Active라 실제 트래픽을 받고 있죠(강조 배지 확인)." 라고 화면을 짚으며 설명.
3. **여기서 Active 리전(KR1) 마스터 터미널로 전환해 강제로 다운시킴** (§3 커맨드 참고).
4. 명령 실행 직후 바로 브라우저로 돌아와서: 몇 초 안에 KR1 카드가 빨갛게 변하는 것을 보여줌
   ("자, 지금 몇 초 만에 리전 카드가 빨갛게 바뀌었죠? 이건 저희 앱이 직접 반대편에 연결해서
   확인한 결과예요.").
5. **바로 이어서**: "그런데 위에 GSLB 배지는 아직 KR1을 가리키고 있어요. 실제 사용자가 보는
   DNS 전환은 TTL 30초 때문에 30~80초 정도 걸립니다 — 이 차이를 보여주려고 이 화면을
   일부러 두 단으로 나눴어요." 라고 말하며 **기다리는 동안 죽지 않는 시간**으로 만들기:
   - 이 타이밍에 CI/CD 탭으로 잠깐 넘어가서 Jenkins→ArgoCD GitOps 흐름을 10~20초짜리로
     짧게 짚어주고 오는 것도 좋음(§2.3에서 다 설명 안 한 CI/CD 파이프라인을 여기서 보충).
   - 또는 "이 프로젝트 왜 신뢰성 있는 거냐면" 하면서 이중화 이야기를 좀 더 풀어도 됨.
6. 다시 `/infra` 탭으로 돌아와 GSLB 배지가 KR2로 넘어간 것 + 새로 생긴 페일오버 이력
   한 줄("KR1 → KR2")과 실시간 지속시간 카운터가 도는 것을 보여줌.
7. 마무리로 KR1을 복구시키고("이제 되돌려볼게요"), 카드가 다시 초록으로 바뀌는 것까지
   보여주면 깔끔하게 닫힘 — failback까지 보여줄 시간이 없으면 여기서 끊고 "failback도
   같은 방식으로 자동 확인되고, 실측 기록은 PROJECT_LOG 4.15절에 남겨뒀습니다"로 대체.

### 2.5 트러블슈팅 하이라이트 (11:00–13:00)

시간이 2분뿐이라 **1개만 깊게, 여유 있으면 1개 더.** 전체 프로젝트 히스토리(38개 절)를
카테고리별로 정리한 전체 목록은 **§7 트러블슈팅 스토리 아카이브**에 있음 — 청중 성향(인프라
배경 vs 개발/CI-CD 배경 vs 프론트엔드 배경)에 맞춰 거기서 골라 쓸 것. 아래는 그중 가장
무난한 기본값:

1. (기본, 1순위) **KR1 Terraform state drift**(4.17절) — "매번 `terraform plan`을 돌릴 때마다
   서버를 통째로 재설치하려고 든" 미스터리를 근본 원인까지 좁힌 이야기. 청중이 개발자라면
   "겉보기 증상과 진짜 원인이 다르다"는 디버깅 서사 자체가 흥미로울 만한 소재.
2. (여유 있으면 2순위) **GSLB Y자 커넥터 UI 버그**(4.38절) — 사용자 피드백("연결선이 끊겨
   보인다")을 3번 잘못 짚고서야 픽셀 단위로 스캔해서 16px짜리 진짜 gap을 찾아낸 이야기 —
   "짐작하지 말고 측정해서 검증한다"는 원칙을 보여주는 짧고 재밌는 소재.

### 2.6 회고 + Q&A (13:00–15:00)

- "지금 남은 건 이 브랜치를 main에 합치고 README를 최신 아키텍처로 갱신하는 마무리
  작업뿐입니다." (진행 상황 투명하게 공유 — `PROJECT_LOG.md` 5절 체크리스트 그대로 언급)
- Q&A 유도 문장: "특히 어떤 트러블슈팅 겪으셨는지 궁금하시면 `PROJECT_LOG.md`에 38개 절
  전부 남아있어서 그 자리에서 같이 찾아봐도 됩니다."

---

## 3. 라이브 데모 실행 커맨드 (실제 타이핑할 것)

**어디서 실행하는지 미리 터미널 탭 이름으로 구분해두는 걸 추천** (예: `KR1-마스터`, `KR2-마스터`, `로컬`).

**KR1 마스터** (현재 Active인 쪽 — 발표 직전에 `/infra`에서 실제 어느 쪽이 Active인지
먼저 확인할 것, GSLB 우선순위 재조정 이력이 있어서 KR1/KR2 어느 쪽이 Active인지는 그때그때
바뀔 수 있음):
```bash
kubectl scale deployment/chaos-demo --replicas=0
```

**복구** (같은 마스터, 데모 마지막 또는 청중이 충분히 봤다고 판단되면):
```bash
kubectl scale deployment/chaos-demo --replicas=3
```

> **주의**: ArgoCD `selfHeal: true`가 켜져 있어서, `replicas=0`으로 내린 변경 자체를
> ArgoCD가 "Git과 다르다"고 판단해 스스로 3으로 되돌릴 수도 있다(자동 복구). 발표 중
> 복구 명령을 치기도 전에 파드 수가 저절로 돌아와 있으면, 당황하지 말고 오히려
> "이건 제가 되돌린 게 아니라 ArgoCD GitOps가 스스로 되돌린 겁니다"로 설명하면 더 좋은
> 포인트가 된다. **어느 쪽이 실제로 벌어지는지는 리허설 때 직접 확인해서 대본에 반영할 것.**

외부에서 실시간 모니터링 창을 하나 더 띄우고 싶으면(제3의 시야, 선택):
```bash
./scripts/06-test-gslb-failover.sh http://www.<본인도메인> 3
```

---

## 4. 사전 준비 체크리스트 (발표 D-1, D-day)

- [ ] **리허설 1회 필수(D-1 권장)** — 실제로 명령을 쳐서 지금 이 순간의 정확한 타이밍(몇 초에
      카드가 빨개지는지, 몇 초에 GSLB가 넘어가는지, 복구는 자동/수동 중 뭐가 실제로
      일어나는지)을 재보고 대본의 시간 문구를 실측값으로 맞출 것.
- [ ] 브라우저 탭 미리 열어두기: `/`(게임), `/infra`, `/cicd`, `docs/ARCHITECTURE.md`
      (또는 GitHub에서 렌더링된 화면)
- [ ] 터미널 탭 미리 열어두고 이름 붙이기: `KR1-마스터`, `KR2-마스터`
- [ ] 발표 시작 전 `/infra`에서 **현재 Active 리전이 어느 쪽인지 확인** — 대본에서
      "KR1을 내린다"고 되어 있어도 실제로 KR2가 Active면 반대로 진행
- [ ] Slack 채널을 화면 한 켠에 띄워두면(선택) 페일오버 관련 알림이 안 오지만, 만약
      다른 알림(예: `KubePodCrashLooping`)이 데모 중 우연히 울려도 당황하지 않기 —
      "이게 저희가 노이즈 알림을 실제로 걸러낸 그 알림 규칙입니다"로 자연스럽게 연결 가능
- [ ] **백업 플랜**: 만약 GSLB 전환이 80초를 넘겨도 안 끝나면(네트워크 컨디션 등),
      "실측으로는 보통 70~80초인데 지금은 좀 걸리네요, 기다리는 동안 트러블슈팅 얘기를
      먼저 할게요"로 자연스럽게 §2.5 순서를 앞당길 것 — **침묵하며 기다리지 말 것.**
- [ ] 발표 종료 후 KR1/KR2 둘 다 정상 상태(Active 트래픽 정상 수신)로 돌아왔는지 마지막으로
      `/infra`에서 확인

---

## 5. 예상 질문 대비 (Q&A)

| 예상 질문 | 답변 근거 |
|---|---|
| 왜 관리형(NKS) 대신 직접 kubeadm으로 클러스터를 구축했나요? | [ARCHITECTURE.md](../ARCHITECTURE.md) 3.1절 — 더 많이 배우고 싶어서, 트러블슈팅 경험 자체가 목적 |
| GSLB랑 일반 로드밸런서(L4/L7)는 뭐가 다른가요? | GSLB는 DNS 레벨에서 리전을 통째로 스위칭 — TTL이 있어서 즉시 전환이 아님(PROJECT_LOG 3.3, 4.15절) |
| 왜 굳이 양쪽 리전에 다 Jenkins/ArgoCD를 놨나요? 비용 아깝지 않나요? | 컨트롤 플레인 이중화(4.34절) — Active 리전 자체가 죽으면 배포 자체가 막히는 SPOF를 없애려는 것 |
| 가장 어려웠던 문제 하나만 꼽으면? | §4에서 준비한 하이라이트 스토리로 자연스럽게 연결 |
| 지금 뭐가 남았나요? | main 병합 + README 갱신 (PROJECT_LOG 5절) |
| 이 알림들 Slack에 실제로 오나요? | 네, `KubePodCrashLooping` 등은 실제로 오고 있고, 구조적으로 항상 참인 etcd/kubeProxy 알림은 걸러냈다(4.38절) |

---

## 6. 시간이 모자랄 때 잘라낼 우선순위 (뒤에서부터 자르기)

1. 트러블슈팅 하이라이트 2번째 스토리
2. CI/CD 탭 곁다리 설명 (§2.4의 "기다리는 동안" 옵션)
3. failback(KR1 복구) 데모 — 말로만 설명하고 스킵
4. 아키텍처 다이어그램 상세 설명 — 그림만 띄우고 한 문장으로 축약

**절대 자르면 안 되는 것: 리전 강제 다운 → 카드 빨개짐 → GSLB 전환 시퀀스 자체.** 이게 이
프로젝트 전체에서 가장 임팩트 있는 한 장면이다.

---

## 7. 트러블슈팅 스토리 아카이브 (전체 히스토리, `PROJECT_LOG.md` 4장 기준)

발표에서 실제로 쓸 건 §2.5에서 1~2개뿐이지만, **여태 프로젝트를 진행하며 겪은 문제 전체를
한눈에 훑어보고 그때그때 청중에 맞는 걸 고르라고** 카테고리별로 전부 정리해둔다. 각 항목
끝의 절 번호로 `PROJECT_LOG.md`에서 원문(증상→원인→해결 전문)을 바로 찾을 수 있다.
"★★★"는 발표 소재로서의 임팩트를 주관적으로 매긴 것 — 서사가 반전이 있거나("겉보기 원인과
진짜 원인이 다르다"), 근본 원인까지 파고든 깊이가 있는 것 위주로 높게 매겼다.

### A. 클라우드 인프라 기초 (Terraform / 네트워크 / 쿼터)

- **Keystone v3 인증 실패**(4.1) — 틀린 비밀번호를 넣어도 똑같은 에러가 뜨는 걸 보고
  "이 메시지 자체가 무의미하다"고 판단, 진짜 원인(v2.0 죽은 엔드포인트, v3는 user_id 인증)을
  찾은 사례. ★★
- **인터넷 게이트웨이 1:1 제약**(4.4) — Terraform provider에 게이트웨이 생성 리소스 자체가
  없어서, 새 VPC 대신 기존 VPC/서브넷을 재사용하도록 설계를 통째로 바꾼 사례. ★
- **Calico VXLANCrossSubnet vs OpenStack anti-spoofing**(4.8) — 파드 IP로 직접 curl해도
  타임아웃 나는 원인을, 클라우드 인프라 레벨부터 하나씩 검증해가며 "CNI와 클라우드의 네트워크
  보안 모델이 충돌"한다는 근본 원인까지 좁힌 사례. **온프레미스면 안 겪었을 문제가 클라우드
  라서 드러난** 좋은 소재. ★★★
- **KR1 Terraform state drift — `image_id` + boot-from-volume**(4.17) — `terraform plan`에
  뜬 이상한 diff 하나를 무시하지 않고 프로바이더 소스 코드까지 읽어서, 방치했다면 **운영 중인
  클러스터 전체가 재설치(`Rebuild()`)로 날아갈 뻔한** 잠재적 대형 사고를 사전에 막은 사례.
  "이상한 diff는 값 자체를 근거로 소스까지 내려가서 확인한다"는 원칙을 가장 잘 보여줌. ★★★ (기본 추천)

### B. 쿠버네티스 스케줄링 / 배포

- **롤링업데이트 교착**(4.9) — replica 수 = 노드 수 + 강제 anti-affinity 조합이 기본
  롤링업데이트 전략(`maxSurge`)과 충돌해 배포가 멈춘 사례. "데모를 위한 설계(노드 분산 시각화)가
  배포 전략과 상성이 나빴다"는 트레이드오프 이야기. ★★
- **Jenkins CrashLoopBackOff — emptyDir 오염**(4.16) — StatefulSet 파드는 재시작해도 볼륨이
  그대로 유지된다는 성질 때문에, init 컨테이너가 "이전 실행의 흔적"에 걸려 넘어지는 자기
  반복적 크래시 루프에 빠진 사례. ★★
- **Jenkins 에이전트 영원히 "접속 대기"**(4.23) — 에러 메시지("포트를 바꾸라")를 액면 그대로
  따랐다가 K8s Service의 고정 포트 배선과 어긋나는 **새 문제를 만들어낸** 반전 있는 사례.
  "에러 메시지를 곧이곧대로 따르기 전에 그 값이 다른 곳에도 고정 배선돼 있는지 확인"이라는
  교훈. 레이어가 여러 겹이라 깊이감 있는 소재. ★★★

### C. CI/CD 파이프라인 (Jenkins / NCR / cosign)

- **NCR 이미지 Pull 412 에러**(4.10) — 401/403이 아니라 412라는 HTTP 상태 코드 하나로
  "인증 문제가 아니라 정책(content-trust) 문제"임을 격리해낸 사례. ★★
- **cosign 서명 3중 장애물**(4.13) — referrers API 미호환(구버전 cosign으로 우회) →
  attestation이 이미지를 멀티 매니페스트로 만듦(`--provenance=false`) → `imagePullPolicy`
  기본값 때문에 캐시된 옛 이미지로 착각할 뻔한 것까지, 서로 무관해 보이는 장애물 3개를
  순서대로 격파한 사례. ★★★
- **Jenkins 첫 end-to-end 4중 장애물**(4.14) — Bitnami 태그 개편, non-root 컨테이너의 셸
  실행 실패, cosign 비밀번호를 로컬에서부터 재현해 원인을 좁힌 것, RBAC `watch` 누락까지
  — "파이프라인의 각 스테이지가 서로 다른 이유로 실패"하는 전형적인 CI/CD 디버깅 서사. ★★★
- **GitHub 웹훅 delivery 미재시도**(4.29) — "push했는데 배포가 안 됨"이 항상 코드 문제는
  아니라는 걸, 웹훅 delivery 로그로 확인한 사례(GitHub는 실패한 delivery를 자동 재시도 안 함). ★

### D. 모니터링 / 알림

- **ServiceMonitor를 Prometheus가 인식 못 함**(4.11) — Helm 차트의 "합리적으로 보이는
  기본값"(`serviceMonitorSelectorNilUsesHelmValues`)이 숨겨놓은 함정을 문서까지 파고들어
  찾은 사례. 에러 로그조차 없어서(조용한 누락) 더 어려웠던 케이스. ★★
- **Alertmanager Slack 연동 3단 실패**(4.12) — `helm --reuse-values`가 `-f` 파일을 무시함 →
  Secret이 다른 네임스페이스에 있었음 → `receivers` 리스트를 통째로 덮어써서 `null` receiver가
  사라져 **Operator의 reconcile 전체가 실패**함, 이 세 레이어를 하나씩 벗겨가며 좁힌 사례.
  이 프로젝트에서 가장 계층이 깊은 디버깅 스토리 중 하나. ★★★

### E. 멀티 리전 / GSLB

- **DNS Plus GSLB — Terraform 커버리지 밖의 콘솔 전용 기능**(4.15) — provider 스키마부터
  확인해 "이건 코드화 못 한다"고 판단하고, "DNS"와 "GSLB"가 콘솔에서 완전히 다른 탭이라는
  것까지 헤매며 알아낸 사례. 최종적으로 실측 80초 failover까지 검증. ★★
- **KR1 재구축 — in-place resize라 클러스터가 그대로 생존**(4.33) — RAM 쿼터가 풀려 정식
  스펙으로 재구축했는데, destroy 없는 in-place resize라 기존 kubeadm 클러스터가 그대로
  살아있어서 재작업을 상당 부분 피한, 사전에 `terraform plan`으로 예측해둔 덕을 본 사례. ★★
- **컨트롤 플레인 이중화 — 4가지 장애물**(4.34) — SPOF(KR2 죽으면 배포 자체 불가)를 없애려고
  KR1에도 Jenkins/ArgoCD/모니터링을 복제하면서 겪은 문제들. §2.3 아키텍처 설명과 직접 연결됨. ★★

### F. 대시보드 UI (가장 최근 작업)

- **ArgoCD 앱 트리 재현 — 권한 확장 없이**(4.36) — 새 RBAC 없이 `ownerReferences`만으로
  Deployment→ReplicaSet→Pod 계층을 복원한 사례. "최소 권한 원칙을 지키면서 기능을 추가한다"는
  이 프로젝트의 일관된 보안 기조를 보여줌. ★★
- **/infra UI 개편 — 장식이었던 상태 표시를 실제 헬스체크로**(4.37) — 대시보드에 Jenkins가
  "정상"으로 떠 있었는데 실제로는 파드가 죽어있었던 사건을 계기로, 색만 있던 상태 표시를 전부
  실제 HTTP 헬스체크로 바꾼 사례 — "장식적 UI가 거짓말을 하고 있었다"는 반전이 있어서 발표에서
  공감을 사기 좋음. ★★★
- **GSLB Y자 커넥터 픽셀 디버깅**(4.38) — 사용자 피드백을 세 번 잘못 짚고 나서야(정렬 문제 →
  색상 대비 문제 → 진짜론 16px짜리 gap) 스크린샷을 픽셀 단위로 스캔해 정량적으로 검증한 사례.
  "느낌으로 고치지 말고 측정해서 검증한다"는 원칙을 가장 극적으로 보여줌. ★★★

### 청중별 추천

| 청중 성향 | 추천 스토리 |
|---|---|
| 클라우드/인프라 배경 | A의 4.8(Calico) 또는 4.17(Terraform drift) |
| 백엔드/CI-CD 배경 | C의 4.14 또는 D의 4.12(Alertmanager 3단) |
| 프론트/UI 배경 | F의 4.37(장식 UI가 거짓말) 또는 4.38(픽셀 디버깅) |
| 누구에게나 무난 | A의 4.17 (반전 있고 이해하기 쉬움) — §2.5의 기본 추천과 동일
