__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
.env
.DS_Store
*.log

# 실제 값이 들어가는 시크릿 - k8s/secret-slack.example.yaml을 복사해서 만들 것
k8s/secret-slack.yaml

# cosign 이미지 서명 개인키 (비밀번호로 암호화돼 있어도 커밋 금지). cosign.pub(공개키)는 커밋해도 됨.
cosign.key

# Terraform 상태/변수 파일 - terraform.tfvars.example을 복사해서 만들 것
terraform/.terraform/
terraform/terraform.tfvars
terraform/*.tfstate
terraform/*.tfstate.*
terraform/crash.log
terraform/override.tf
terraform/override.tf.json
